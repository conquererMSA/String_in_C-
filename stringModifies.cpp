#include<bits/stdc++.h>
using namespace std;

int main(){
    string a;
    string b;
    cin>>a;
    cin>>b
    //string modifiers related function
    //string concat a=a+b, a+=b, a.append(b)
    //** a[5]='A' size barbe na. and 
    //cout<<a; //Hello pabo, HelloA pabo na. karon size bare ni
    //a=a+'A' ba a+='A' ba a[5]='A' kora zabe na. evabe memory expand hoy na.
    // memory epanding
    //b='A' => a+=b; ba a.push_back('A') kora zabe.
    //a.pop_back()=> delete last ndex value.
    //a.assign("Value")
    //a.erase(startIndex, howMany)=> givenIndex theke baki sob value delete hoye zabe
    //a.replace(starIndex, howMany, "Cay");
    //a.repalce(2,0,"PPA")=> ekahne kichui delete hobe na. Hel er por PPA bosbe and lo dane sore zabe.=> result hobe HelPPAlo
    //a.insert(givenIndex, "Value")=> insert a value in specific position.
    //a.insert(2,"Kappa")=>HelKappalo
    return 0;
}